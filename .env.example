# Create RESEND_API_KEY in Resend, then add these values in Vercel Project Settings.
# Never commit the real .env file or any live key.
RESEND_API_KEY=re_replace_with_your_resend_api_key
RESEND_FROM_EMAIL=Ellis Services Group <onboarding@resend.dev>
